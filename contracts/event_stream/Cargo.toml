[package]
name = "event_stream"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
soroban-sdk = "22.0.0"
serde = { version = "1.0", features = ["derive"] }

[profile.release]
opt-level = "s"
